const config = {
  plugins: [
    "@tailwindcss/postcss",   // Add this line for Tailwind support
    "autoprefixer",   // Add this line to automatically add vendor prefixes for cross-browser compatibility
  ],
};

export default config;
